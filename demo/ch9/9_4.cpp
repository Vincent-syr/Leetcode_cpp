//9_4.cpp
#include <iostream>
#include <iomanip>
#include "9_3.h"
using namespace std;




int main(int argc, char const *argv[])
{
    Array<int>  A(10);	// 用来存放质数的数组，初始状态有10个元素。
    int n;	//质数范围的上限，运行时输入
    int primecount = 0, i, j;
    return 0;
}
